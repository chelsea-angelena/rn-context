import { ThemeProvider } from 'react-native-elements';

const theme = {
	Button: {
		backgroundColor: 'red',
	},
	font: {
		fontFamily: 'Roboto',
	},
	colors: {
		primary: 'red',
	},
};

export default { theme };
